# No application-specific environment variables are currently required.
#
# Add public variables as NEXT_PUBLIC_* and validate them in app/env.ts.
# Keep server-only secrets unprefixed and never commit their values.
