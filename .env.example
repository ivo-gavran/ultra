# The bundled same-origin mock implements the current users API. Replace this
# with the absolute NestJS API URL when the real backend is available.
NEXT_PUBLIC_API_URL=/api/mock

# Keep server-only secrets unprefixed and never commit their values.
